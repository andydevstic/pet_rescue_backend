table! {
    users (id) {
        id -> Int4,
        email -> Varchar,
        full_name -> Varchar,
        password -> Text,
        role_id -> Int4,
        address -> Text,
        phone_number -> Varchar,
    }
}
